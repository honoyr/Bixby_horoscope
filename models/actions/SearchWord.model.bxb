action (Search) {
  description (search for a word in a dictionary)
  collect {
    input () {
      type (Sign)
      min (Required)
      max (One)
    }
  }
  type (Search)
  output (SignSearchResult)
}
